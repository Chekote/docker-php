# chekote/php-lumen-${SUPPORTED_LUMEN_VERSION}:${PHP_VERSION}-${REVISION}
ARG PHP_VERSION=7.4
FROM chekote/php:${PHP_VERSION}-lumen5.6

ENV SUPPORTED_LUMEN_VERSION '6.0'
ENV MIN_PHP_VERSION '7.2.0'

RUN set -eu; \
    #
    check_php_version ${MIN_PHP_VERSION}; \
    #
    # Make sure everything works
    php -v; \
    php-fpm${PHP_VERSION} -v
